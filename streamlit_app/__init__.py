"""Streamlit frontend for the Flipkart Order Intelligence & Support Center.

This package is a UI layer only. It never recomputes a model prediction, a
retrieval score, or a policy answer — every page calls the real Part 1/2/3
functions and displays what comes back.
"""
