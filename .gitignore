# Python environments
.venv/
.venv-*/
venv/
__pycache__/
*.pyc
*.pyo

# Secrets — never commit real credentials
.env
.env.*
!.env.example

# Raw Fashion-MNIST IDX download (regenerated automatically by torchvision).
# The exported PNG samples in data/sample_images/ ARE committed.
data/FashionMNIST/

# Cached frozen-backbone feature tensors (large, regenerated by part2/cache_features.py)
data/feature_cache/

# OS noise
.DS_Store
